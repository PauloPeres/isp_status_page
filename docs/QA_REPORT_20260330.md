# QA Report — 2026-03-30

## Issues Found & Fixed

| # | Issue | Severity | Status |
|---|-------|----------|--------|
| 1 | SLA list crashes — backend returns `slas` key, frontend expects `items` | CRITICAL | FIXED |
| 2 | Status pages list crashes — same API key mismatch | CRITICAL | FIXED |
| 3 | Login/register fields — no spacing between label and input box | MINOR | FIXED |
| 4 | No user profile editing page | MODERATE | FIXED |
| 5 | Dark/light mode toggle hidden inside sidebar menu | LOW | EXISTS (in sidebar header) |
| 6 | Timezone is free-text input; no language selector | MODERATE | FIXED |
| 7 | Dark mode — dark-on-dark text in cards, secondary color, toolbar | MODERATE | FIXED |
| 8 | Dashboard SLA card too minimal — just 3 chips | LOW | FIXED |
| 9 | Settings exposes SMTP/channel configs to org admins | MODERATE | FIXED |
| 10 | Subscribers and Email Logs are stub "Coming Soon" pages | MODERATE | FIXED |
| 11 | API response key mismatch across all list services | CRITICAL | FIXED (11 services) |

## Fixes Applied

### Issues 1, 2, 11: API Response Format Mismatch (CRITICAL)
Fixed 11 Angular services to map backend entity-specific keys to standardized `{ items, pagination }`:
- `sla.service.ts` — maps `slas`
- `status-page.service.ts` — maps `status_pages`
- `integration.service.ts` — maps `integrations`
- `alert-rule.service.ts` — maps `alert_rules`
- `escalation.service.ts` — maps `escalation_policies`
- `maintenance.service.ts` — maps `maintenance_windows`
- `scheduled-report.service.ts` — maps `scheduled_reports`
- `user.service.ts` — maps `users` + `invitations`
- `api-key.service.ts` — maps `api_keys`
- `check.service.ts` — maps `checks`
- `activity-log.service.ts` — maps `activity_log`

### Issue 3: Auth Form Spacing
- Changed `--padding-start: 0` to `--padding-start: 12px` in login and register components
- Added `--inner-padding-end: 12px` and `--min-height: 56px`

### Issue 4: User Profile Page
- Created `profile.component.ts` with username/email display, language selector (en/pt_BR/es), timezone dropdown, password change form
- Added `/profile` route and "My Profile" sidebar link (person-circle-outline icon)

### Issue 6: Timezone & Language
- Replaced free-text timezone input in settings with `ion-select` dropdown (13 common timezones)
- Added per-user language and timezone in the new Profile page

### Issue 7: Dark Mode Text Visibility
- Added `--ion-color-secondary` overrides in dark theme mixin
- Added `--ion-toolbar-color: #F8FAFC`
- Added global `body.dark` rules for card titles, segment buttons, searchbar, labels

### Issue 8: Dashboard SLA Card
- Replaced minimal chips with rich SLA overview: large numbers for Compliant/At Risk/Breached with color coding
- Added stacked bar visualization showing proportions
- Shows "No SLAs configured" empty state

### Issue 9: Settings Security
- Removed "Channels" tab entirely from org settings (SMTP, Telegram, Webhook configs)
- These remain in the super admin settings panel only

### Issue 10: Subscribers & Email Logs
- Created `subscriber.service.ts` and rewrote `subscriber-list.component.ts` with search, list, badges, delete, skeleton loading
- Created `email-log.service.ts` and rewrote `email-log-list.component.ts` with search, list, status badges, error display, skeleton loading

## Build Status
All changes compile successfully. Only pre-existing unused import warnings remain.
